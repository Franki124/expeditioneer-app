import 'package:flutter/widgets.dart';

class AppRadii {
  AppRadii._();

  static const cardSmall = 12.0;
  static const cardLarge = 18.0;
  static const buttonSmall = 9.0;
  static const buttonLarge = 14.0;
  static const pill = 100.0;
  static const input = 9.0;

  static BorderRadius card = BorderRadius.circular(cardLarge);
  static BorderRadius button = BorderRadius.circular(buttonLarge);
  static BorderRadius pillShape = BorderRadius.circular(pill);
  static BorderRadius inputShape = BorderRadius.circular(input);
}
